<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<style>
.card {
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
  max-width: 300px;
  margin: auto;
  text-align: center;
  font-family: arial;
}

.title {
  color: grey;
  font-size: 18px;
}

button {
  border: none;
  outline: 0;
  display: inline-block;
  padding: 8px;
  color: white;
  background-color: #000;
  text-align: center;
  cursor: pointer;
  width: 100%;
  font-size: 18px;
}

a {
  text-decoration: none;
  font-size: 22px;
  color: black;
}

button:hover, a:hover {
  opacity: 0.7;
}
</style>
</head>
<body>
<%= link_to "ホーム", root_path , :class => "btn btn-success btn-lg"%>  <%= link_to "情報を変更", edit_user_registration_path , :class => "btn btn-info btn-lg"%>
<br>
<hr>
<h2 style="text-align:center">User Profile</h2>

<div class="card">

  <% if current_user.avatar.attached? %>
      <%= image_tag current_user.avatar, style: 'width:100%'%>
  <% end %>

  <h1>[<%= current_user.role %>] <%= current_user.name %></h1>
  <p class="title"><%= current_user.age %>歳</p>
  <div style="margin: 24px 0;">
    <a href="#"><i class="fa fa-dribbble"></i></a> 
    <a href="#"><i class="fa fa-twitter"></i></a>  
    <a href="#"><i class="fa fa-linkedin"></i></a>  
    <a href="#"><i class="fa fa-facebook"></i></a> 
  </div>
  <button type="button" class="btn btn-danger"><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></button>
</div>
</body>
</html>