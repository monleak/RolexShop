<p style="color: green"><%= notice %></p>

<h1 class="px-4">Watches</h1>

<div class="row" id="watches">
  <% @watches.each do |watch| %>
    <%= render watch %>
  <% end %>
</div>

<h3 class="p-4">
  <%= link_to "New watch", new_watch_path %>
</h3>
